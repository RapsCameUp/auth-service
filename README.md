# Auth Service

Authentication microservice handling user login, registration, and token verification.

## Endpoints

- `GET /health` - Health check
- `POST /auth/login` - User login
- `POST /auth/register` - User registration
- `GET /auth/verify` - Token verification

## Run

```bash
npm install
npm start
```
